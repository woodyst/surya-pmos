From 2de5a6247f7cb575797ed5f218a593929f64d7b1 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Eduardo=20Garc=C3=ADa-M=C3=A1dico=20Portabella?=
 <woodyst@gmail.com>
Date: Mon, 13 Jul 2026 00:03:17 +0200
Subject: [PATCH 011/106] Revert: remove smart-amp AFE call from sm8250.c
 startup (again)

The port-lifecycle fix (q6afe_dai_get_port() instead of get_from_id+
put) did not resolve the regression - hw_params still fails with
-EINVAL and PipeWire still loops suspended->error on-device,
identically to the first attempt. This means the earlier port-
refcount theory was wrong or at least incomplete: something more
fundamental about sending this AFE param during .startup - before
the port's own real I2S config has been sent via hw_params/prepare -
is putting the DSP port into a state that then rejects the real
config. Reverting again rather than guessing further live on the
device; needs to be revisited off-device (e.g. try from .prepare/
.trigger after q6afe_port_start(), not from .startup) before trying
again.
---
 sound/soc/qcom/sm8250.c | 24 ------------------------
 1 file changed, 24 deletions(-)

diff --git a/sound/soc/qcom/sm8250.c b/sound/soc/qcom/sm8250.c
index 7b78f60bc..abdf4302d 100644
--- a/sound/soc/qcom/sm8250.c
+++ b/sound/soc/qcom/sm8250.c
@@ -117,30 +117,6 @@ static int sm8250_snd_startup(struct snd_pcm_substream *substream)
 			MI2S_BCLK_RATE, SNDRV_PCM_STREAM_PLAYBACK);
 		snd_soc_dai_set_fmt(cpu_dai, fmt);
 		mi2s_set_codec_dai_fmt(rtd, codec_dai_fmt);
-
-		/*
-		 * EXPERIMENTAL: register TI's TAS256x smart-amp AFE module
-		 * with the DSP, mirroring the downstream vendor driver's
-		 * afe_tas_smartamp_set_calib_data() call - not known to be
-		 * required for basic playback (see surya audio investigation
-		 * notes), testing whether it changes anything at all.
-		 * Uses the DAI's own already-open port (q6afe_dai_get_port())
-		 * rather than opening/closing a second reference to it - a
-		 * prior attempt that did the latter corrupted the port state
-		 * and broke the whole pipeline.
-		 */
-		{
-			struct q6afe_port *port = q6afe_dai_get_port(cpu_dai);
-			u32 smartamp_payload[14] = { 1, };
-			int ret;
-
-			ret = q6afe_send_generic_param(port,
-				0x11111112 /* AFE_SMARTAMP_MODULE_RX */,
-				0x10012D13 /* CAPI_V2_TAS_RX_ENABLE */,
-				smartamp_payload, sizeof(smartamp_payload));
-			dev_info(cpu_dai->dev,
-				"tas smartamp enable experiment: %d\n", ret);
-		}
 		break;
 	case QUINARY_MI2S_RX:
 		codec_dai_fmt |= SND_SOC_DAIFMT_NB_NF | SND_SOC_DAIFMT_I2S;
-- 
2.43.0

